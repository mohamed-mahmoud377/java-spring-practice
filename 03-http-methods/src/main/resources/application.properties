spring.application.name=03-http-methods
# The API listens here. Change if 8081 is taken.
server.port=8081
# Include our ResponseStatusException reasons in the JSON error body so the front-end
# (and curl) can display *why* a request failed, e.g. "Admin only".
server.error.include-message=always
